/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // PWA configuration
  // Note: Per un setup completo PWA, considera next-pwa
  // Per ora manteniamo semplice con manifest.json
};

module.exports = nextConfig;

